apiVersion: v1
kind: Service
metadata:
  name: apuestas-service
  labels:
    app: apuestas-service
spec:
  type: ClusterIP
  selector:
    app: apuestas-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8005